{
  "name": "my-health-probe-func",
  "version": "1.0.0",
  "description": "TypeScript Azure Function experiment and POC",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start --typescript --port 3000",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "14.x",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
